- hosts linux
  vars:
    training:
      topic: ansible
      tainer: sathish
      labs: aws
  tasks:
  - name: print dict varaiable
    debug:
      msg: "{{ training['topic'] }}"
  - name: print another variable
    debug:
      msg: "{{ training.topic }}"
